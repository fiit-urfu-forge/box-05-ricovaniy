<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="using:LLMWiki.App.ViewModels"
        x:Class="LLMWiki.App.Views.ConflictResolutionWindow"
        x:DataType="vm:ConflictResolutionViewModel"
        Title="Разрешение конфликтов"
        Width="1100" Height="720">

    <Grid RowDefinitions="*,Auto" Margin="12">
        <ItemsControl Grid.Row="0" ItemsSource="{Binding Items}">
            <ItemsControl.ItemTemplate>
                <DataTemplate DataType="vm:ConflictResolutionItem">
                    <Border BorderBrush="LightGray" BorderThickness="1" Padding="8" Margin="0,0,0,12" CornerRadius="4">
                        <StackPanel>
                            <TextBlock Text="{Binding RelativePath}" FontWeight="Bold" />
                            <Grid ColumnDefinitions="*,*" Margin="0,8" RowDefinitions="Auto,*">
                                <TextBlock Grid.Row="0" Grid.Column="0" Text="Моя версия" FontStyle="Italic" />
                                <TextBlock Grid.Row="0" Grid.Column="1" Text="Версия из GitHub" FontStyle="Italic" />
                                <Border Grid.Row="1" Grid.Column="0" Margin="0,4,4,0"
                                        BorderBrush="LightGray" BorderThickness="1">
                                    <ScrollViewer>
                                        <SelectableTextBlock Text="{Binding Entry.LocalContent}"
                                                             FontFamily="Cascadia Mono, Consolas, monospace" />
                                    </ScrollViewer>
                                </Border>
                                <Border Grid.Row="1" Grid.Column="1" Margin="4,4,0,0"
                                        BorderBrush="LightGray" BorderThickness="1">
                                    <ScrollViewer>
                                        <SelectableTextBlock Text="{Binding Entry.RemoteContent}"
                                                             FontFamily="Cascadia Mono, Consolas, monospace" />
                                    </ScrollViewer>
                                </Border>
                            </Grid>
                            <StackPanel Orientation="Horizontal" Spacing="8">
                                <RadioButton GroupName="{Binding RelativePath}"
                                             Content="Оставить мою"
                                             IsChecked="{Binding Choice, Converter={x:Static vm:ConflictChoiceToBoolConverter.KeepLocal}}" />
                                <RadioButton GroupName="{Binding RelativePath}"
                                             Content="Взять из GitHub"
                                             IsChecked="{Binding Choice, Converter={x:Static vm:ConflictChoiceToBoolConverter.TakeRemote}}" />
                            </StackPanel>
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>

        <Button Grid.Row="1"
                HorizontalAlignment="Right"
                Content="Применить все решения"
                IsEnabled="{Binding AllResolved}" />
    </Grid>
</Window>
